import {
  sanityClient,
  urlFor,
  usePreviewSubscription,
  PortableText,
} from "../../lib/sanity";

const OneRecipe = () => {
  return (
    <div>
      <h1>This is slug</h1>
    </div>
  );
};

export default OneRecipe;
